# =============================================================================
# SecureDx API — Production Dependencies
# All versions pinned for reproducible builds.
# Update with: pip-compile requirements.in
# =============================================================================

# --- Web Framework ---
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# --- Database ---
sqlalchemy==2.0.27
alembic==1.13.1
asyncpg==0.29.0          # Async PostgreSQL driver
psycopg2-binary==2.9.9   # Sync driver (for Alembic migrations)

# --- Authentication & Security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
httpx==0.26.0            # For Keycloak token introspection

# --- Validation & Serialization ---
pydantic==2.6.1
pydantic-settings==2.1.0
email-validator==2.1.0

# --- FHIR ---
fhir.resources==7.1.0    # FHIR R4 resource models

# --- Cryptography & Privacy ---
cryptography==42.0.2
opendp==0.9.2            # Differential privacy

# --- De-identification ---
# MIST is used as a subprocess (installed separately in Docker)
# Fallback: presidio for simpler de-id
presidio-analyzer==2.2.354
presidio-anonymizer==2.2.354

# --- ML / Inference Client ---
onnxruntime==1.17.1
numpy==1.26.4
scikit-learn==1.4.0      # For preprocessing / normalization
shap==0.44.1             # Explainability

# --- Logging & Monitoring ---
structlog==24.1.0
python-json-logger==2.0.7

# --- Utilities ---
python-dateutil==2.8.2
pytz==2024.1
tenacity==8.2.3          # Retry logic for external calls
aiofiles==23.2.1
faker==22.6.0  # For seed script
shap==0.44.1
onnxruntime==1.17.1
apscheduler==3.10.4
